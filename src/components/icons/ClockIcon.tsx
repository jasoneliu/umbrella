import React from "react";
import { MaterialCommunityIcons } from "@expo/vector-icons";

const ClockIcon = () => {
  return (
    <MaterialCommunityIcons name="clock-outline" size={36} color="white" />
  );
};

export default ClockIcon;
